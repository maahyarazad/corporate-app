import { space, spaceNoPX } from "./spacing";
import { colors } from "./colors";
import { fontSizes, fontWeights } from "./fonts";

export const theme = {
  space,
  colors,
  fontWeights,
  fontSizes,
  spaceNoPX,
};
