import React from "react";
import { Animated, StyleSheet, View } from "react-native";

export const HeaderImage = ({ y }) => {
  return <Animated.Image></Animated.Image>;
};

const styles = StyleSheet.create({
  container: {},
});
